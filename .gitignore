# ── secrets (NEVER commit) ──
.env
.env.local
*.key
*.pem
# .env.example はテンプレートなので追跡する(下で明示)
!.env.example

# ── local tooling / editor ──
.claude/
.vscode/
.idea/
.DS_Store

# ── python ──
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# ── generated artifacts (regenerable) ──
# 論文HTML(paper1.html/paper2.html)は build_paper_html.py で再生成可だが、
# 配布用成果物として追跡する。
persona_*.json
culture_result_*.json
data/gss/
