# ── secrets (NEVER commit) ──
.env
.env.local
*.key
*.pem
# .env.example はテンプレートなので追跡する(下で明示)
!.env.example

# ── local tooling / editor ──
.claude/
.vscode/
.idea/
.DS_Store

# ── python ──
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# ── intermediate work docs (kept locally, not published) ──
docs/paper2_cmr_report.md
docs/repo_reorg_plan.md
docs/paper2_gss_spec_v0.2.md
docs/internal_notes.md
docs/gss_validation_findings.md
docs/ess_validation_findings.md
docs/ess_validation_plan.md
docs/paper2_gss_ess_spec_v0.3.md

# ── paper manuscripts (kept locally, not published; regenerate HTML via build scripts) ──
docs/paper1_media_generation.md
docs/paper1.html
docs/paper2_contextual_mode_resolver.md
docs/paper2.html

# ── local archive (deprecated/intermediate data + junk; kept locally, not published) ──
data/_archive/

# ── Track B persona UI (local-only for now; may move to its own repo) ──
ui/

# ── generated artifacts (regenerable) ──
persona_*.json
culture_result_*.json
data/gss/
data/ess/
