# Culture層・Community層(LLM)で使用。各自のキーを入れて .env にコピー。
# .env 自体は .gitignore 済み(コミットされない)。
OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-4o
# 任意(Claude を使う場合)
# ANTHROPIC_API_KEY=sk-ant-...
# CLAUDE_MODEL=claude-sonnet-4-6
